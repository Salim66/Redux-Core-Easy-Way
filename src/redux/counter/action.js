
// make increment action method
export const makeIncrement = (payload) => {
    return {
        type: "INCREMENT"
    }
}

// make decrement action method
export const makeDecrement = (payload) => {
    return {
        type: "DECREMENT"
    }
}